{
    "name": "ipfs-uploader",
    "version": "1.0.0",
    "description": "A utility for uploading files to IPFS and storing the file hashes in a smart contract on Bitnet",
    "main": "src/index.ts",
    "scripts": {
        "build": "tsc",
        "start": "node build/index.js"
    },
    "dependencies": {
        "axios": "^1.2.0",
        "dotenv": "^16.0.3",
        "ethers": "^5.7.1"
    },
    "devDependencies": {
        "@types/node": "^22.4.1",
        "typescript": "^4.8.4"
    }
}
